# Development only — not needed to run Ultron, and deliberately kept out of
# requirements.txt so the shared setup stays as small as possible.
#
#   venv\Scripts\python.exe -m pip install -r requirements-dev.txt
#   venv\Scripts\python.exe -m pytest

pytest>=8.0
